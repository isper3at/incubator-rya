TODO write this

Mention we only support CentOS 7.

For the binary artifact, include the manual instructions that tell you how to 
install the query manager as a service.

Need to untar the tar.gz into the '/opt' directory.
Need to yum install 'jsvc'.
Need to move the service file into the '/etc/systemd/system' directory.